const mongoose = require("mongoose");
require("../model/user");
const User = mongoose.model("User");
const jwt = require("jsonwebtoken");

/**
 * Function handling add product request
 */

exports.addproduct = (req, res) => {};

exports.editproduct = (req, res) => {};
